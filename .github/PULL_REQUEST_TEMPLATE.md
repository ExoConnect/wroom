<!--
PRs are limited to project collaborators — see CONTRIBUTING.md.
PRs from non-collaborators are closed automatically.
If you're not a collaborator, open an issue instead.
-->

## What

## Why

## Verification

- [ ] `cargo check` / `cargo clippy` / `cargo test` pass
- [ ] `pnpm --filter web build` passes (if web touched)
- [ ] Perf claims backed by a benchmark or metric
- [ ] `DECISIONS.md` untouched, or the change is approved in the linked issue
